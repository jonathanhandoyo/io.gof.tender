'use strict';

angular.module('mainApp')
    .controller('ProjectLocationController', function ($scope, $rootScope, $stateParams, $http, $timeout, Upload, $window, $location) {
        $scope.init = function(){

        }
    });